% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_psbart_binary.R
\name{.fit_psbart_binary}
\alias{.fit_psbart_binary}
\title{Fit Principal Stratification BART (Single Chain)}
\usage{
.fit_psbart_binary(
  X,
  Y,
  Z,
  W,
  n_warmup = 1000L,
  n_samples = 1000L,
  save_trees = FALSE,
  k = 2,
  n_trees = 200L,
  verbose = FALSE,
  n_initial = 0L
)
}
\arguments{
\item{X}{A scaled numeric matrix of covariates with propensity \code{e}
already appended as the last column.}

\item{Y}{A binary outcome vector (0/1).}

\item{Z}{A binary treatment assignment/instrument vector (0/1).}

\item{W}{A binary treatment uptake/received vector (0/1).}

\item{n_warmup}{Number of warmup/burn-in iterations (default: 1000).}

\item{n_samples}{Number of posterior samples to collect (default: 1000).}

\item{save_trees}{Logical; save tree structures for prediction (default: FALSE).}

\item{k}{Prior hyperparameter for node mean prior (default: 2).}

\item{n_trees}{Number of trees in the BART ensemble (default: 200).}

\item{verbose}{Logical; print progress (default: FALSE).}

\item{n_initial}{Number of initial iterations using MoM offsets (internal, default: 0).}
}
\value{
A list containing:
\item{imputed}{Array of imputed compliance class memberships with dimensions
(n_samples, n, 2) and named variables: "nt" (never-takers), "at" (always-takers)}
\item{probs}{Array of posterior probabilities and outcome means with dimensions
(n_samples, n, 6) and named variables: "p_a" (P(always-taker)), "p_n" (P(never-taker)),
"m_y0c" (\eqn{E[Y(0) \mid complier]}), "m_y1c" (\eqn{E[Y(1) \mid complier]}),
"m_y0n" (\eqn{E[Y(0) \mid never-taker]}), "m_y1a" (\eqn{E[Y(1) \mid always-taker]})}
\item{trees}{Tree structures if \code{save_trees = TRUE}}
}
\description{
Internal function that fits a single chain of the principal stratification
BART model. Users should typically use \code{\link{prince_BART}} instead,
which handles data preparation and runs multiple chains.
}
\keyword{internal}
