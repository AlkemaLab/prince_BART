% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/general_bart.R
\name{general_BART_overlap}
\alias{general_BART_overlap}
\title{Generalizability Overlap Analysis for PATE Estimates}
\usage{
general_BART_overlap(
  object,
  threshold = NULL,
  overlap_value = c("zero", "NA"),
  verbose = FALSE
)
}
\arguments{
\item{object}{A \code{general_pate} object from \code{\link[=general_BART]{general_BART()}}}

\item{threshold}{Numeric threshold for trimming based on standardized
selection scores. Observations with \verb{|e_s_tilde| > threshold} are
excluded. Default is NULL (no trimming, just compute overlap).}

\item{overlap_value}{Value to replace \code{tau} with for trimmed observations.
Either \code{"zero"} (set to 0) or \code{"NA"} (exclude completely). Default is \code{"zero"}.}

\item{verbose}{Logical; print progress messages. Default is FALSE.}
}
\value{
A list with components:
\describe{
\item{overlap}{Data frame with overlap metrics: pi_c, pi_t, pi_s, e_s_tilde}
\item{e_s_tilde_source}{Standardized selection scores for source data}
\item{n_trimmed}{Number of observations trimmed (if threshold used)}
\item{pate_trimmed}{Trimmed PATE estimate (if threshold used)}
\item{ci_trimmed}{95 percent CI for trimmed PATE (if threshold used)}
}
}
\description{
Compute generalizability overlap scores and optionally trim observations
with low overlap to produce a trimmed PATE estimate.
}
\examples{
\dontrun{
fit <- general_BART(princebart_fit, newdata, ...)
overlap <- general_BART_overlap(fit, threshold = 2)
}

}
\seealso{
\code{\link[=general_BART]{general_BART()}}, \code{\link[=general_BART_transportability]{general_BART_transportability()}}
}
